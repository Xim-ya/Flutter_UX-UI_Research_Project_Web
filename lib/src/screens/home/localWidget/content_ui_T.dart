import 'package:flutter/material.dart';

class ContentUIT extends StatelessWidget {
  const ContentUIT({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Content UI TABLET"),
    );
  }
}
