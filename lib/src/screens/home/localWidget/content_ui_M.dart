import 'package:flutter/material.dart';

class ContentUIM extends StatelessWidget {
  const ContentUIM({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Content UI MOBILE"),
    );
  }
}
