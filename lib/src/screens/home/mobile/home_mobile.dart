import 'package:flutter/material.dart';

class HomeScreenM extends StatelessWidget {
  const HomeScreenM({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("HomeScreen Mobile"),
    );
  }
}
