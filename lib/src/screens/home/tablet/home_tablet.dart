import 'package:flutter/material.dart';

class HomeScreenT extends StatelessWidget {
  const HomeScreenT({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Home Screen Tablet"),
    );
  }
}
