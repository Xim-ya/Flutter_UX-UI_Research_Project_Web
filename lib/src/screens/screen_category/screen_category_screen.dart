import 'package:flutter/material.dart';

class ScreenCategoryScreen extends StatelessWidget {
  const ScreenCategoryScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Column(
        children: [
          Text("Screen Category Screen"),
        ],
      ),
    );
  }
}
